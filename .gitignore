# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Databases — knowledge.db is a Phase 15 artifact; build it, don't commit it.
database/*.db
database/*.db-shm
database/*.db-wal
*.sqlite
*.sqlite3

# OS / editor
.DS_Store
.idea/
.vscode/

# Build
build/
dist/
out/
target/

# Phase 3b human drop directory — transient staging for files downloaded from
# the ORC. phase3_ingest_drop.py copies matched files into data/raw/<subject>/,
# so nothing here needs to be versioned.
data/incoming/
