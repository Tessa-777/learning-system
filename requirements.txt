# Runtime and test dependencies for the Phase 1 bootstrap layer.
#
# Install in a virtual environment:
#   python3 -m venv .venv
#   . .venv/bin/activate
#   pip install -r requirements.txt
#
# pytest is used to run the schema/logging test suite.

pytest>=7.0
PyYAML>=6.0
jsonschema>=4.0
# Pass 1 extraction reads the text layer of the organized source PDFs
# (scripts/build_physics_pass1.py) and hashes them for provenance.
pypdf>=4.0
